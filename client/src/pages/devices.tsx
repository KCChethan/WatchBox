import Dashboard from "./dashboard";

// For now, devices page is the same as dashboard
// In a larger app, this could be a different view (table, filters, etc.)
export default function Devices() {
  return <Dashboard />;
}
